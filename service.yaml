apiVersion: v1
kind: Service
metadata:
  name: flask-app-service
  namespace: monitoring # Or whatever namespace your app is in
spec:
  selector:
    app: flask-app # Must match the pod label
  ports:
    - name: http
      port: 80
      targetPort: 5000 # The port your Flask app exposes metrics on
